## Live Demo
https://book-inventory2.netlify.app/


## Note on Backend Data

This project uses JSON Server for local development to simulate backend APIs.
Since the frontend is deployed on Netlify, the JSON Server runs only on localhost.
For production use, a hosted backend service would be required to persist and share data across devices.

